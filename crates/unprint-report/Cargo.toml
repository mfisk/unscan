[package]
name = "unprint-report"
version = "0.1.0"
edition = "2021"

[dependencies]
unprint = { path = "../.." }
unprint-fonts = { path = "../unprint-fonts" }
image = "0.25"
base64 = "0.22.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
